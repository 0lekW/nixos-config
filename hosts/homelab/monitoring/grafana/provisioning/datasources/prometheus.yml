apiVersion: 1
datasources:
  - name: prometheus
    type: prometheus
    # Pinned on purpose: dash1 and dash2 reference this exact UID.
    # Change it and every panel shows "no data".
    uid: eeyqekat80mwwb
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
